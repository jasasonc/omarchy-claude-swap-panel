{
  "schemaVersion": 1,
  "id": "io.github.jasasonc.claude-swap-panel",
  "name": "Claude Swap Panel",
  "version": "1.1.0",
  "author": "Jaša Sonc",
  "license": "MIT",
  "description": "The Agents panel with a tab for each claude-swap account: usage limits per account, and a keyboard-first switch between accounts. Unofficial, not part of claude-swap.",
  "kinds": ["bar-widget"],
  "activation": "on-demand",
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Claude Swap Panel",
    "description": "Usage limits for every claude-swap account, and a guarded switch between them.",
    "category": "AI",
    "aliases": ["agents", "model-usage", "claude-swap", "cswap", "claude-swap-panel"],
    "allowMultiple": false,
    "defaults": {
      "providers": {
        "claude": { "enabled": true },
        "codex": { "enabled": true },
        "fireworks": { "enabled": true }
      },
      "refreshIntervalSec": 900,
      "cswapRefreshIntervalSec": 180
    },
    "schema": [
      { "key": "refreshIntervalSec", "type": "integer", "label": "Refresh interval (seconds)", "min": 30, "max": 3600, "step": 30, "defaultValue": 900 },
      { "key": "cswapRefreshIntervalSec", "type": "integer", "label": "claude-swap refresh interval (seconds)", "min": 60, "max": 3600, "step": 30, "defaultValue": 180, "description": "How often to read the usage of all claude-swap accounts." }
    ]
  },
  "omarchy": {
    "clonedFrom": "omarchy.agents"
  }
}
